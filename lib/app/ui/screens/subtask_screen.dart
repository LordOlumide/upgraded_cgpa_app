import 'package:flutter/material.dart';

class SubTaskScreen extends StatelessWidget {
  const SubTaskScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sub-Task Screen'),
      ),
      body: ListView(
        children: [],
      ),
    );
  }
}
